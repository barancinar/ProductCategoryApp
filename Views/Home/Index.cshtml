@model List<Category>
@{
    ViewData["Title"] = "Product Categories";
}
<h2>Product Categories</h2>
<ul>
    @foreach (var category in Model)
    {
        <li>
            <a asp-action="CategoryProducts" asp-route-category="@category.Name">@category.Name</a>
        </li>
    }
</ul>
